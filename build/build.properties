# Properties file for Build

# Specify the path to the folder that contains all the shared libraries.
#

stage.dir=stage
dist.dir=dist
stage.classes.dir=stage/classes
src.dir=src
config.dir=conf
test.dir=test
reports.dir=${stage.dir}/reports
test.reports=${reports.dir}
javadocs.dir=${stage.dir}/javadocs

shared.lib = ../ext/lib/java/
utils.lib=../utils/dist